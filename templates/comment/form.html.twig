{{ form_start(form_comment, { 'action': path('comment_create' , { 'id' : comment.article.id }), 'method': 'POST' }) }}

    {{ form_widget(form_comment) }}
    <button type="submit" class="btn btn-default">Add comment</button>
{{ form_end(form_comment) }}
